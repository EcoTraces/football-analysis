# The ML service currently takes no external configuration — it is a pure
# function of the inputs the backend sends it (no database, no API keys).
# This file exists so future config (e.g. a model registry path once
# training/backtesting land) has a documented place to go.

PORT=8000
